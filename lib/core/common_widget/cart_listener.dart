import 'package:flutter/material.dart';

/// A wrapper widget to listen for shopping cart changes.
class CartListener extends StatelessWidget {
  final Widget child;
  const CartListener({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    // Placeholder for now, just returns child.
    return child;
  }
}
